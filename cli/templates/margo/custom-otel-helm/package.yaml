apiVersion: non-margo.org
kind: ApplicationPackage
metadata:
  name: otel-demo-pkg
  labels:
    env: dev
  annotations:
    description: "A web application for running otel demo app"
spec:
  sourceType: GIT_REPO
  source:
    # url: "http://52.224.241.180:3000/gogsadmin/otel"
    # branch: "master"
    url: {{URL}}
    branch: {{BRANCH}}
    #commit: <commit id>
    #token: <pass-pat>